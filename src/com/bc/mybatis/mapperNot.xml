<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Nbookstore">
	<select id="Nselectall" resultType="com.bc.model.vo.GuestBookVO">
		SELECT * FROM NOTICE
		ORDER BY N_NUM DESC
	</select>
	<insert id="Ninsert" parameterType="map">
		INSERT INTO NOTICE VALUES(SEQNOTICE.nextval,#{subject},#{content},SYSDATE,#{kind})
	</insert>
	<select id="Nselectone" resultType="com.bc.model.vo.GuestBookVO" parameterType="int">
		SELECT * FROM NOTICE
		WHERE N_NUM = #{nNum}
	</select>
</mapper>